# Application port
PORT=8080

# Session secret for connect-flash and express-session
SESSION_SECRET="superSecretKey"

# Database profile (not actively used for in-memory, but demonstrates config)
# Options: h2 (default), mysql, postgres
DATABASE_PROFILE=h2
