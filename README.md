# Hello world with TypeScript :computer:

This is a simple **"Hello world"** done with **TypeScript** programming language.

## Source code

This is the source code of the program:


Notice that `console.log("Hello world!");` shows the string `"Hello world!"` on the screen.

## Compile and Execute program

To compile the "Hello World" program (optional, if you want to run plain JavaScript):

First, ensure you have TypeScript installed:

Then, compile the program:

This will generate `HelloWorld.js`. To execute the compiled JavaScript program:

Alternatively, you can run the TypeScript program directly using `ts-node` without an explicit compilation step, which is common for simple scripts:

