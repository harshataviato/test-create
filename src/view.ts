/**
 * Represents the view component for the Hello World application.
 * Responsible for displaying the message.
 */
export class HelloWorldView {
  /**
   * Displays the message to the console.
   * @param message The message to display.
   */
  displayMessage(message: string): void {
    console.log(message);
  }
}
