# Hello World with TypeScript

This is a simple "Hello World" application written in TypeScript, demonstrating a basic MVC structure.

## Prerequisites

*   Node.js (version 18 or higher recommended)
*   npm (Node Package Manager, usually included with Node.js)

## Setup

1.  **Install dependencies:**

    ```bash
    npm install

2.  **Run the application:**

    ```bash
    npm start
.
├── README.md         // This file
├── package.json      // Project dependencies and scripts
├── tsconfig.json     // TypeScript compiler configuration
├── src
│   ├── index.ts      // Main entry point of the application
│   ├── controller.ts // Controller logic
│   ├── model.ts      // Data model (if needed)
│   └── view.ts       // View logic
└── dist              // Compiled JavaScript output (created after running `npm run build`)

