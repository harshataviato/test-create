# For more information, please see: https://aka.ms/devcontainer.json

# Use Node.js 18 on Alpine Linux as the base image for a lightweight environment.
FROM mcr.microsoft.com/devcontainers/javascript-node:18-alpine

# [Optional] Install any other tools needed. For example, install PostgreSQL client tools.
RUN apk add --no-cache postgresql-client

# [Optional] Uncomment to install a different version of a package, or a package not installed by the base image.
# For example, to install a specific Node.js version management tool if not already present.
# RUN su vscode -c "umask 0002 && . /usr/local/share/nvm/nvm.sh && nvm install 18"

# [Optional] Uncomment when using a ptrace-based debugger like C++.
# RUN apk add --no-cache gdb

# [Optional] Uncomment to set the default user.
# USER vscode
