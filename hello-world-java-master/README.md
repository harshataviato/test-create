# Hello World with TypeScript :globe_with_meridians:

This is a simple **"Hello World"** application demonstrating a basic Model-View-Controller (MVC) structure using **TypeScript**, **Node.js**, and **Express.js**.

## Directory Structure


## Setup and Run

To set up and run this project, follow these steps:

### 1. Environment Setup

Make sure you have Node.js and npm (Node Package Manager) installed on your system.
You can download them from the official Node.js website: [nodejs.org](https://nodejs.org/)

To check if Node.js and npm are installed, run:


### 2. Dependency Installation

Navigate to the project directory and install the required dependencies:


### 3. Build the TypeScript Project

Compile the TypeScript code into JavaScript:


This will create a `dist` directory containing the compiled JavaScript files.

### 4. Running the Server

You can run the application in two ways:

#### a. Production Mode (using compiled JavaScript)


#### b. Development Mode (with hot-reloading using `ts-node-dev`)


Once the server is running, open your web browser and navigate to `http://localhost:3000` to see "Hello world from TypeScript!".
