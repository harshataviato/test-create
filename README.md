# Hello World with TypeScript

This is a simple "Hello World" application written in TypeScript.

## Prerequisites

*   Node.js (>= 18) and npm (Node Package Manager) installed.

## Setup

1.  Install dependencies:

    ```bash
    npm install

2.  Run the compiled JavaScript file:

    ```bash
    npm run start
npm run dev

