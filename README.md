# Mobile BFF Project Setup and Execution

This document outlines the steps to set up, install dependencies, and run the Mobile BFF application.

## Environment Setup

Ensure you have the following installed:

*   **Node.js**: Version 18 or higher (LTS recommended).
*   **Yarn**: Version 1.22 or higher.
*   **Docker**: If you plan to run local dependencies or for certain development setups.

## Dependency Installation

Navigate to the project root and install the required dependencies using Yarn:


## Running the Server

To start the development server for the `bff` application:


The application will typically be available at `http://localhost:3000`.

To run tests:


To run architecture tests:


To run a specific feature's tests, for example, `hello-world`:

