/**
 * Model representing the Hello World data.
 * In a real-world scenario, this might interact with a database.
 */
class HelloModel {
  /**
   * Returns the core message logic.
   * @returns {string} The greeting message.
   */
  static getGreeting() {
    return "Hello world!";
  }
}

module.exports = HelloModel;
