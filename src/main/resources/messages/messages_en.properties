// This is a Java properties file used for Internationalization (i18n) in Spring applications.
// The file is intentionally empty, indicating that message look-ups for English will fall
// back to the default `messages.properties` file.
//
// This is not Java code and cannot be converted to TypeScript directly.
// In a Node.js/TypeScript project using a library like `i18next`, this would typically mean:
// - There is no `src/locales/en/translation.json` file.
// - Or, the `src/locales/en/translation.json` file exists but is empty, or explicitly points
//   to the default language for missing keys.
// - The i18n configuration would define a fallback language (e.g., 'en' falls back to 'default' or 'en-US').
//
// Example conceptual `src/locales/en/translation.json` (if it existed but was empty for fallback):
/*
{
  // This file is empty; messages will fall back to the default locale.
}
*/
# This file is intentionally empty. Message look-ups will fall back to the default "messages.properties" file.
