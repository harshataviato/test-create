{
  "name": "harshataviato-test-create-ebb4653",
  "version": "1.0.0",
  "description": "A simple Node.js application demonstrating MVC pattern with Express, Sequelize, and Handlebars.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "migrate": "npx sequelize-cli db:migrate",
    "migrate:undo": "npx sequelize-cli db:migrate:undo",
    "migrate:undo:all": "npx sequelize-cli db:migrate:undo:all",
    "seed:all": "npx sequelize-cli db:seed:all",
    "test": "NODE_ENV=test mocha --timeout 5000 --exit",
    "test:watch": "NODE_ENV=test mocha --timeout 5000 --watch --exit"
  },
  "keywords": [
    "node",
    "express",
    "sequelize",
    "handlebars",
    "mvc",
    "sqlite"
  ],
  "author": "Google Senior Engineer",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-handlebars": "^7.1.2",
    "method-override": "^3.0.0",
    "sequelize": "^6.37.3",
    "sequelize-cli": "^6.6.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "mocha": "^10.4.0",
    "chai": "^5.1.1",
    "supertest": "^7.0.0"
  }
}
